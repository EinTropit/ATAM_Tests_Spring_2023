.global _start

  .section .text

  mov $destination, %rax
  cmpb $189, 0(%rax)
  jne bad_exit
  cmpb $248, 1(%rax)
  jne bad_exit
  cmpb $54, 2(%rax)
  jne bad_exit
  cmpb $187, 3(%rax)
  jne bad_exit
  cmpb $104, 4(%rax)
  jne bad_exit
  cmpb $4, 5(%rax)
  jne bad_exit

  movq $60, %rax
  movq $0, %rdi
  syscall

bad_exit:
  movq $60, %rax
  movq $1, %rdi
  syscall

.section .data
  num: .int 5
  source: .byte 189,248,54,187,104,0
  destination:
              .zero 5
              .byte 4


